<%= form_for(@consultum) do |f| %>
  <% if @consultum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consultum.errors.count, "error") %> prohibited this consultum from being saved:</h2>

      <ul>
      <% @consultum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :medico %><br />
    <%= f.collection_select :medico_id, Medico.all, :id, :name, :prompt => true %>
  </div>

  <div class="field">
    <%= f.label :fecha_cita %><br>
    <%= f.datetime_select :fecha_cita %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
